class messages:
    buffer = {}
    def __init__(self, sender, receiver, text):
        self.sender = sender
        self.receiver = receiver
        self.text = text

class chats:
    chats_list = {}
    def __init__(self, message):
        if 
        self.companions = (message.sender, message.receiver)
